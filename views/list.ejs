<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My To-Do List 🌸</title>

  <!-- Import Google Fonts for nice clean text -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300..700&display=swap" rel="stylesheet" />

  <!-- Link to external stylesheet for all the pretty colors and layout -->
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <!-- Header with calm, encouraging message and a gentle emoji 🌸 -->
  <header>
    <h1>Softly moving forward, one step at a time 🌸</h1>
  </header>

  <!-- Main container holding the task input, filters, and the list -->
  <main class="container">
    <!-- Form to add new tasks, making sure input names match server expectations -->
    <form action="/add" method="POST" class="task-form">
      <input type="text" name="taskText" placeholder="Enter a new task..." />
      <select name="priority">
        <!-- Priority options with emojis for quick visual -->
        <option value="High">High 🔥</option>
        <option value="Medium">Medium 🌼</option>
        <option value="Low">Low 🍃</option>
      </select>
      <button type="submit">➕ Add</button>
    </form>

    <!-- Filter form to let users pick which priority tasks to show -->
    <!-- Server remembers the selected option so it stays highlighted -->
    <form action="/filter" method="POST" class="filter-form">
      <select name="filterPriority">
        <option value="All" <%= selectedPriority === 'All' ? 'selected' : '' %>>All</option>
        <option value="High" <%= selectedPriority === 'High' ? 'selected' : '' %>>High 🔥</option>
        <option value="Medium" <%= selectedPriority === 'Medium' ? 'selected' : '' %>>Medium 🌼</option>
        <option value="Low" <%= selectedPriority === 'Low' ? 'selected' : '' %>>Low 🍃</option>
      </select>
      <button type="submit">🔍 Filter</button>
    </form>

    <!-- Unordered list showing all todos -->
    <ul class="todo-list">
      <% todos.forEach((todo, index) => { %>
        <!-- Each task gets a CSS class based on priority to color it nicely -->
        <li class="<%= todo.priority.toLowerCase() %>">
          <!-- Display the task text and the priority label in smaller font -->
          <span><%= todo.text %> <small>(<%= todo.priority %>)</small></span>
          
          <!-- Action buttons for delete and edit -->
          <div class="actions">
            <!-- Delete button inside a small form to POST the deletion -->
            <form action="/delete/<%= index %>" method="POST" style="display:inline;">
              <button type="submit" title="Delete">🗑️</button>
            </form>
            <!-- Edit link opens a separate page to modify the task -->
            <a href="/edit/<%= index %>" title="Edit">✏️</a>
          </div>
        </li>
      <% }) %>
    </ul>
  </main>

  <!-- Footer with a gentle reminder and earthy emoji 🍂 -->
  <footer>
    <p>Slow is beautiful, steady is strong 🍂</p>
  </footer>


   <!-- Alert script to stop empty tasks from submitting -->
  <script>
    const form = document.querySelector(".task-form");
    form.addEventListener("submit", function(e) {
      const input = form.querySelector("input[name='taskText']");
      if (!input.value.trim()) {
        e.preventDefault(); // stop form from submitting
        alert("Task cannot be empty!");
      }
    });
  </script>

  
</body>
</html>

